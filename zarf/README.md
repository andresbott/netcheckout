# zarf/

Supporting files that are not application code: packaging, build, deploy, and
test-harness assets.

Examples of what lives here as the project grows:

- packaging configs (e.g. `.deb`/nfpm inputs) referenced by `.goreleaser.yaml`
- Docker build files for reproducible builds
- end-to-end / integration test harnesses
- sample-data or dev helpers

Empty for now — this file only keeps the directory tracked in git.
